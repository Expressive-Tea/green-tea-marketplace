{
  "name": "green-tea-marketplace",
  "type": "module",
  "private": true,
  "version": "26.9.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node scripts/deploy.mjs --self-test && node --test scripts/registry.test.mjs",
    "deploy": "npm run build && node scripts/deploy.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.6",
    "js-yaml": "^4.3.2",
    "sanitize-html": "^2.17.7"
  }
}
